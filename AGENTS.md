# Pi 全局宿主环境说明

以下规则适用于当前用户在 Pi 中打开的所有项目；项目根目录的 `AGENTS.md` 可以补充更具体的项目约定。用于减少 LLM 常见编码错误的行为准则。可与项目特定指令合并使用。

**权衡说明：** 这些准则倾向于谨慎而非速度。对于琐碎的任务，请自行判断。

## 速查：六条底线

1. **不伪造成功**：真实失败必须显式暴露，不用默认值、空对象、mock、旧链路或跳过检查掩盖。隔离 mock 仅可用于已标明边界的测试或演示，不得冒充真实业务依赖。
2. **完成前必须验证**：必须说明实际结果；未完成必要验证时不得声称完成或通过，并写明原因、未覆盖项和剩余风险。
3. **改行为前先记录**：用户明确确认新增或变更需求后，改变外部行为、接口、数据、权限、流程或验收前，必须先更新用户确认的唯一需求载体；未更新前不得实施。
4. **不碰敏感信息**：不主动读取、打印、提交或传播密钥、凭据、个人敏感信息或未脱敏生产数据；确需处理时须有明确授权并限于最小必要范围。
5. **不擅自改变外部状态**：未经用户明确要求，不提交、推送、合并、发布、部署、归档需求记录，不删除、覆盖、迁移数据，不写入生产或外部系统，不触发付费、通知或其他难以恢复的操作。
6. **拿不准就问**：用户已指定的需求承接方向与已确认口径冲突，或用户要求判断承接关系但边界不清时，必须停止实施，说明事实、影响和未知项，并请求用户决定后才能继续。
## 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

实现之前：
- 明确说明你的假设。如果不确定，询问。
- 如果存在多种解释，呈现它们 —— 不要默默选择。
- 如果存在更简单的方法，说出来。适时提出异议。
- 如果有不清楚的地方，停下来。指出困惑点。询问。

**询问与自主的边界：** bug 修复和有明确正确答案的工作自主推进到底；影响接口、数据或架构的取舍才停下来问。

## 2. 简洁优先

**用最少的代码解决问题。不要过度推测。**

- 不要添加要求之外的功能。
- 不要为一次性代码创建抽象。
- 不要添加未要求的"灵活性"或"可配置性"。
- 不要为不可能发生的场景做错误处理。
- 如果写了 200 行代码而其实 50 行就够，重写它。

写代码前，按顺序停在第一个成立的选项（来源优先级梯子）：

1. 这需要存在吗？不需要就跳过（YAGNI）
2. 代码库里已经有？复用现成的 helper / util / 模式，不要重写
3. 标准库能做？用它
4. 平台原生特性能覆盖？用它（`<input type="date">` 优于日期选择器库、CSS 优于 JS、DB 约束优于应用层代码）
5. 已安装的依赖能解决？用它。几行代码能搞定的事，绝不引入新依赖
6. 一行能写完？写一行
7. 以上都不行，才写最少的可用代码

梯子在理解问题之后运行，不能替代理解：先读改动会触及的代码、追完真实流程，再选梯级。

简化的边界：
- 永不简化掉：信任边界的输入校验、防数据丢失的错误处理、安全措施、无障碍基础、用户明确要求的东西。
- 刻意砍角且有已知天花板的简化（全局锁、O(n²) 扫描、朴素启发式），用 `debt:` 注释标明天花板和升级路径（如 `# debt: 全局锁，吞吐量成瓶颈时改每账户锁`）。

自问：资深工程师会觉得这过于复杂吗？如果是，简化。

## 3. 目标驱动执行

**定义成功标准。循环验证直到达成。任务没有被证明可运行之前，绝不标记为完成。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入编写测试，然后让它们通过"
- "修复 bug" → "编写重现 bug 的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都能通过"

对于多步骤任务，说明一个简短的计划：
```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

完成前：跑测试、看日志、用事实证明正确性；必要时对比主干和你的改动之间的行为差异。自问："高级工程师会批准这段改动吗？"

强有力的成功标准让 LLM 能够独立循环执行。弱标准（"让它工作"）需要不断澄清。

## 4. 实施边界

- 改变外部可观察行为、接口契约、数据结构、权限、状态流转、验收标准或用户路径前，必须先更新用户确认的唯一需求载体。
- 修改前必须区分**目标变更**与**非目标不变式**；未要求改变的行为、接口、数据、权限、错误处理、性能、布局、调用方式和兼容性默认不变。
- 只改变实现目标所必需的内容，不通过放宽校验、吞错、默认成功、绕过权限、扩大默认权限、削弱类型约束或破坏兼容性制造局部效果。
- 必须改变非目标不变式时，先说明原因、影响范围和验证方式；未经用户确认不得实施。
- 修改前识别工作区和目标文件中的已有改动，包括相关、未提交或与目标行重叠的内容；不得擅自覆盖、回退、删除或格式化。无法安全合并时停止并说明。
- 用最少代码解决当前问题，不为一次性或假想需求增加抽象与配置，不顺手重构或清理既有死代码。
- 注释解释意图、边界和非显然副作用，不复述代码；临时方案或技术债务用 `TODO:` / `FIXME:` 标明背景和影响。
- 新增、迁移或调整导出接口、DTO、后端响应类型、公共类型、服务 API 或其他契约型类型时，必须保留已有接口说明与字段注释；来源缺失时只按用户确认的契约、接口文档或已有代码事实补充最小必要说明，不得凭字段名猜测业务含义，无法确认的字段应显式标注来源或未确认边界。
- 源码、配置和文档默认 UTF-8 无 BOM；中文项目新增内容默认简体中文，写入后读取确认无乱码或错误转码。

## 5. 正确性、安全与验证要求

- 优先保证业务正确性、边界清晰和可测试性；遵循项目既有架构、目录、命名与调用方式。引入依赖、服务、技术栈或绕过既有依赖前，说明原因、影响和验证成本。
- 生产和真实业务链路不得用 mock、本地缓存、seed、旧字段或旧链路冒充真实依赖；测试、演示和本地开发的模拟数据必须隔离并标明边界。
- 只有业务上确实无数据时才能返回空；不可读、不可解析、契约缺失、接口失败或权限失败都属于错误，不得用默认值、猜测值、空对象或默认成功掩盖。
- 面向用户的错误信息不得泄露内部敏感细节。
- 展示日志、报错、配置或命令输出前先检查敏感信息；发现疑似泄露时停止扩散，只说明风险和位置，不复制敏感内容。
- 不把 `.env`、凭据、连接串或令牌写入源码、文档、示例、测试快照、日志或提交说明；不伪造权限、凭据或安全校验结果。
- 可能重试、跨系统或影响真实业务的关键写操作必须采用幂等或等效防重机制，并确认写入结果。
- 验证必须覆盖本次改动及直接影响；涉及结构、交互、输入校验、服务、权限、数据契约或外部集成时，扩展到直接调用方和关键反向路径。纯文案或局部呈现可以做最小验证。
- 优先使用项目已有的测试、类型检查、lint、构建或手工验收命令；项目固定运行时或工具链版本时先按约定切换。
- 项目固定 Node.js 版本时，源码构建、测试、lint、格式化及 npm/pnpm/yarn 脚本使用项目声明版本；不参与构建产物的 OpenSpec、CodeGraph 等外部流程 CLI 可按工具官方要求使用独立运行时，执行前说明边界并验证可用。
- 不得通过跳过、删除或弱化测试，缩小必要验证范围，或仅修改断言和期望值来制造通过结果。
- 完成前重新运行能够证明结论的验证，读取完整结果；失败、无法执行或覆盖不足时，说明原因、影响、未覆盖项和剩余风险。
- 完成后至少说明实际改变、已核对的直接相关不变式、验证证据、未验证项和剩余风险；涉及外部状态或难恢复操作时，还必须说明是否执行、授权来源和结果。


## 宿主环境与跨平台命令

- 当前 Pi 宿主：Windows（`win32`，`x64`）；内置 `bash` 通常在 Bash 中执行，扩展调用 `pi.exec` 时直接启动进程。
- 执行命令前先确认当前 shell、项目脚本和工具链；不要默认 Linux、路径格式或包管理器。
- Windows 查找命令使用 `where.exe <command>`；Bash 使用 `command -v`，不要只依赖 `which`。
- Windows 下全局 npm CLI 可能由 `.cmd` shim 提供；直接启动 CLI 时选择当前平台可用入口。
- 不硬编码 `/usr/bin`、`/tmp` 等 POSIX 路径；使用项目或运行时提供的路径 API。
- 宿主环境不等于目标运行环境；涉及 WSL、容器、CI 或原生依赖时，先确认实际执行环境。
