## 本地数据结构驱动的 API 适配原则

进行接口对接和数据适配时，必须遵循 **Local Schema First（本地结构优先）** 原则。

### 核心原则

**本地数据结构是 Target Schema，是数据适配的唯一目标；API Response 只是数据来源。**

始终按照以下方向思考：

`Local Model → 确定需要的数据 → 在 API Response 中寻找对应来源 → 映射/转换 → Local Model`

而不是：

`API Response → 分析接口有哪些字段 → 决定本地如何存储`

### 执行规则

1. **先读取本地数据结构**

   * 优先分析现有 `type`、`interface`、schema、model 以及实际业务使用方式。
   * 明确本地每个字段的名称、类型、语义和用途。
   * 不要先根据 API Response 建立数据模型。

2. **以本地字段为起点寻找 API 数据**

   * 对本地需要的字段逐项分析。
   * 在 API Response 中寻找语义对应的数据来源。
   * 字段名不要求一致，以实际业务语义为准。

   思考方式必须是：

   > “本地字段 X 需要什么数据？API 中哪个字段或哪些字段可以提供它？”

   而不是：

   > “API 有字段 X，我应该把它放到本地哪里？”

3. **所有差异由 Adapter / Mapper 消化**
   包括但不限于：

   * 字段重命名
   * 类型转换
   * 嵌套结构展开
   * 多字段组合
   * 枚举转换
   * 时间格式转换
   * 默认值处理
   * 空值处理
   * 数据清洗

4. **忽略 API 多余字段**

   * API 返回但本地模型不需要的数据，不要因为“接口提供了”就加入本地结构。
   * 不允许 API Response 反向污染本地模型。

5. **处理本地需要但 API 不直接提供的数据**

   * 优先寻找可以组合、转换或推导的数据。
   * 无法获得时，根据本地模型约定使用 `undefined`、`null`、默认值或显式缺失处理。
   * 不得为了迁就 API 而随意删除或修改本地字段。

6. **保持业务层和 UI 层与 API 解耦**

   * UI 和业务逻辑只依赖 Local Model。
   * 原始 API Response / DTO 不应直接进入 UI 或核心业务逻辑。
   * API 结构变化时，原则上只修改 Adapter / Mapper。

### 数据流

正确：

`API Response → Adapter → Local Model → Business/UI`

错误：

`API Response → Business/UI`

错误：

`API Response → 修改 Local Model 以迎合 API → Business/UI`

### 实现前的检查顺序

每次进行接口适配时，严格按照以下顺序：

1. 找到并读取本地目标数据结构。
2. 明确本地实际需要哪些字段。
3. 查看 API Response。
4. 为每个本地字段寻找 API 数据来源。
5. 确定字段映射和转换规则。
6. 在 Adapter / Mapper 中完成转换。
7. 输出严格符合本地结构的数据。
8. 检查业务层和 UI 是否仍然只依赖本地模型。

### 最重要的判断标准

当不知道应该如何适配时，始终问：

**“本地模型需要什么，API 能从哪里提供？”**

不要问：

**“API 提供了什么，本地应该怎么改？”**

除非明确要求修改本地模型，否则不得因为接口字段、层级、命名或类型不同而调整本地数据结构。
